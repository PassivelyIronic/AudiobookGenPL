# ============================================================
#  Audio-Książnica - zależności
#  Python 3.11+
# ============================================================

# --- API & ASGI server -------------------------------------------------------
fastapi>=0.115.2
uvicorn[standard]==0.32.0
python-multipart>=0.0.18        # obsługa upload-u plików w FastAPI
aiofiles==24.1.0                # async file I/O dla upload endpointu

# --- Konfiguracja i walidacja ------------------------------------------------
pydantic>=2.11.10
pydantic-settings==2.6.0
python-dotenv==1.0.1

# --- Kolejka zadań -----------------------------------------------------------
celery==5.4.0
redis==5.1.1                    # broker + result backend

# --- Parsowanie EPUB ---------------------------------------------------------
EbookLib==0.18
beautifulsoup4==4.12.3
lxml==5.3.0                     # szybki parser HTML/XML dla BS4

# --- TTS (XTTS-v2 / Idiap fork) ----------------------------------------------
# UWAGA: PyTorch instalujemy OSOBNO przed coqui-tts, bo od 0.27.4 nie jest
# bundled. Komenda dla CUDA na Windowsie:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
#
# coqui-tts to aktywnie utrzymywany fork projektu Coqui (Idiap Research
# Institute) po upadku oryginalnej firmy. Import API zostaje:
#   from TTS.api import TTS
coqui-tts>=0.27.5,<0.29.0

# --- Frontend ----------------------------------------------------------------
gradio>=6.0.0,<7.0.0

# --- Narzędzia developerskie / testy -----------------------------------------
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
httpx==0.27.2                   # wymagane przez FastAPI TestClient